// sizeof() Operator.
// Example 2: Operation with an expression.

#include<stdio.h>

int main(){
    float a = 10.10;
    double b = 20.10;

    printf("size of (a+b) is %d", sizeof(a+b));
    return 0;
}
